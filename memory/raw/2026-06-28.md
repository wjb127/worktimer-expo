# raw 작업 로그 — 2026-06-28

원문 작업 기록(요약 최소화). 지시 전 자동 읽기 금지. 읽을 땐 wc -c 후 구간만.

---
## 18:47 — 안드로이드 실기기 빌드·E2E·실버그 수정 (Galaxy A16)

### 배경
- 직전: iOS 실기기에서 "폰에서 제대로 작동 안됨" 보고. 리서치로 실기기 E2E 방법론 정리(→ `07-physical-device-e2e.md`).
- 이번 세션: 사용자가 **안드로이드 폰 연결** → "빌드 재개 ㄱㄱ". A안(e2e 게이팅 + 빌드) 실행.

### 코드 변경 (미커밋)
1. `src/screens/LoginScreen.tsx` — dev-login 게이트 `{__DEV__ && ...}` → `{(__DEV__ || process.env.EXPO_PUBLIC_E2E === '1') && ...}`. testID `dev-login-button` 유지.
2. `eas.json` — `e2e` 프로필 추가:
   ```json
   "e2e": { "extends": "preview", "distribution": "internal",
            "env": { "EXPO_PUBLIC_E2E": "1" }, "android": { "buildType": "apk" } }
   ```
3. `src/lib/api/sessions.ts` — **실버그 픽스**(아래 참조).

### 환경 확인
- adb 처음엔 device 안 보임 → `adb kill-server && adb start-server` 후 잡힘: **RF9XB01KRLH, SM-A165N(Galaxy A16), Android 16**, USB 디버깅 인증됨.
- 폰에 깔린 건 **Expo Go(host.exp.exponent)뿐** → 커스텀 네이티브 모듈(google-signin/live-activity) 때문에 Expo Go 불가 → dev/standalone APK 필요.
- ANDROID_HOME=`~/Library/Android/sdk` 존재, Java 21 → **로컬 Gradle 빌드 가능**.
- `npx eas`는 패키지명 틀림 → `eas-cli` 필요(이번엔 로컬빌드라 안 씀).

### 빌드 삽질 → 디스크 풀이 진범
- `npx expo prebuild --platform android` 성공 → `android/` 생성. (withFmtFix는 'ios' 가드라 안드로이드 무영향)
- 1차 `./gradlew :app:assembleDebug` 실패: "Failed to release lock on daemon addresses registry" (락 에러).
- `./gradlew --stop` → 데몬 없음. 2차 `--no-daemon` 재시도 → **진짜 원인: `java.io.IOException: No space left on device`**.
- `df`: Data 볼륨 거의 풀. 회수: `rm -rf ~/Library/Developer/Xcode/DerivedData/*`(15G) + `npm cache clean --force` + `pnpm store prune` → **+70Gi 확보**. (락 에러도 디스크 풀 증상이었음 — 락파일조차 못 씀)
- 3차 빌드 중 사용자가 "느림/끊어달라" → 빌드+Metro kill(이번에 띄운 것만, Docker/Firestore/기존 8082 보존).

### 램/스왑 정리 (별도 이슈)
- 진단: 32GB RAM, **스왑 54GB 중 53.5GB 사용/716MB 남음** → 스래싱. 빈RAM 9%. uptime 16일.
- 주범: **claude 세션 31개** + worktimer-expo 좀비(스테일 Metro 8082 PID 9072, jest-worker 고아).
- 좀비 정리 후에도 스왑 안 빠짐(게으른 회수).
- 사용자 지시 "크롬 싹 + claude 이거 하나 빼고 다 날려":
  - 내 claude PID = **87773**(조상 워크 chain으로 확정: bash48021→claude87773→Warp). 가드: mine 못찾으면 claude 손 안 댐.
  - `osascript quit Chrome` + `pkill -9 -f "Google Chrome"` + `pkill -9 -f app_mode_loader`(PWA 카카오/Grok).
  - claude 30개 kill(87773만 spare). 잔여 PID 342 = km-201 죽은세션의 배포 명령 물고 도는 고아쉘 → 배포중이라 안 건드림.
  - **결과: 스왑 53.5GB→31.8GB(−22GB), 빈RAM 9%→21%**. 스래싱 해소.

### 빌드 재개 → 성공
- 폰 재확인(RF9XB01KRLH), 디스크 67Gi.
- `./gradlew :app:assembleDebug --no-daemon --console=plain` → **BUILD SUCCESSFUL in 44s**(증분, 캐시 덕). app-debug.apk 139M.
- ⚠️ `| tail` 파이프는 EOF까지 버퍼링 → 백그라운드 빌드 진행 안 보임. 다음엔 tail 없이 출력파일 직접 읽기.

### 폰 연결 (USB 터널 — 서브넷 문제 소멸)
- `adb install -r app-debug.apk` → Success, pkg `kr.codeatlas.worktimer` 설치 확인.
- 8081·8082 둘 다 **Docker 점유** → Metro는 빈 포트 **8085**.
- `adb reverse tcp:8085 tcp:8085`(device 8085 → mac 8085). 처음 `tcp:8081 tcp:8085`로 했다가 실패(아래).
- `CI=1 npx expo start --port 8085` 로 Metro 기동(비대화).

### 앱 로딩 함정 2개
1. 디버그 빌드라 **Expo Dev Launcher** 먼저 뜸(서버 URL 선택 대기) → 번들 요청 안 옴.
   - scheme 확인: `dumpsys package` → `exp+worktimer-expo`.
   - 딥링크: `adb shell am start -a android.intent.action.VIEW -d "exp+worktimer-expo://expo-development-client/?url=http%3A%2F%2Flocalhost%3A8081"`.
2. 첫 딥링크 url=8081 → 화면에 **"Unable to load script"**(스샷으로 확인). 원인: dev-client가 manifest는 받았는데 Metro가 **번들 URL을 자기 포트 8085로 광고** → 폰이 localhost:8085 요청 → 터널은 8081만 → 실패.
   - 해결: 터널을 **Metro 포트와 일치**(`adb reverse tcp:8085 tcp:8085`) + 딥링크 url=8085.
   - → **Android Bundled 4848ms index.ts (1325 modules)** 성공. Dev Launcher에서 8085·8081 둘 다 초록불.
- Dev Launcher 홈에서 8085 행 탭(비율좌표 540,620) → 앱 로드. Dev Menu 오버레이 "Continue"/back으로 닫음.

### E2E (Maestro 실기기 구동)
- `maestro test .maestro/full-flow.yaml` (Running on RF9XB01KRLH):
  - dev-login-button 탭 COMPLETED → "타이머.*" assert COMPLETED(=백엔드 /auth/dev-login→JWT→메인 진입)
  - 타이머/기록/통계/설정 탭 전부 COMPLETED + 스샷
  - **마지막 스텝만 FAILED**: 설정화면 `scrollUntilVisible "로그아웃"` 못 찾음. → 앱버그 아님, 안드로이드 셀렉터 이슈(미해결, 자잘).
- `timeout`은 macOS에 없음(gtimeout). maestro 자체 타임아웃으로 충분.

### 🐛 실버그 발견·수정 (시각이 진실)
- 타이머 화면 스샷 하단 토스트: **`getOngoingSession error: SyntaxError: JS...`** ← "폰에서 작동 안됨"의 정체.
- 원인: `src/lib/api/sessions.ts` `apiGetOngoing` — 진행중 세션 없을 때 백엔드가 **200 + 빈 본문**(NestJS null 직렬화). `res.ok` true라 가드 못 타고 `res.json()` → 빈문자열 파싱 → SyntaxError.
- 시뮬 Maestro는 네비게이션만 보고 넘어가 못 잡음. 실기기 화면 육안으로 잡힘.
- 픽스:
  ```ts
  const text = await res.text();
  if (!text) return null;
  const data = JSON.parse(text);
  return data ? mapSession(data) : null;
  ```
- Metro CI모드는 fast-refresh off → 정상모드 재기동(`npx expo start --port 8085`) 후 딥링크 reload.
- **검증(스샷)**: reload 후 토스트 사라짐. 인증상태 유지(SecureStore 토큰)로 재로그인 없이 타이머 진입.
- 긍정 케이스: 시작 탭 → "업무 중"+빨간 종료(apiStart OK, 알림권한 다이얼로그 정상) → 종료 탭 → "대기 중" + **오늘 총 업무 시간 00:00:28** 기록. create→ongoing→end→total 풀사이클 라이브 백엔드 왕복 OK.
- `npx tsc --noEmit` 통과(에러 0).

### 현재 라이브 상태
- Metro 8085(fast-refresh) 백그라운드 가동중, 앱이 폰에서 USB 연결로 살아있음.
- adb reverse: `tcp:8081 tcp:8085`, `tcp:8085 tcp:8085`.

### 미결/다음
- 커밋 안 함(android/ gitignore 여부 확인 후 결정). 변경: sessions.ts(픽스), LoginScreen, eas.json.
- Maestro full-flow 로그아웃 스텝 안드로이드용 셀렉터 수정 필요.
- 출시 전 운영 `DEV_LOGIN_ENABLED` OFF(기존 미결).

---
## 19:54 — codeatlas 어드민 구축·배포 + 구글 로그인 수정

### 어드민 콘솔 (신규 프로젝트 ss-037-codeatlas-admin)
- 결정: 확장형 멀티앱 어드민(별도 프로젝트) / Supabase→실제론 직접 Postgres / 듀얼비번(admin/admin123!)
- DB(codeatlas, 신규 테이블만): login_event(IP/기기), admin_users(admin/admin123! bcrypt 시드), admin_login_attempts. users 무변경.
- ★ 함정: supabase-js는 PostgREST 경유 → codeatlas 스키마 미노출이라 PGRST106 실패 → **postgres.js 직접연결**로 전환(role codeatlas_app, session pooler 5432, prepare:false). 신규 테이블 GRANT to codeatlas_app.
- 스택: Next16+Tailwind4+postgres.js+bcryptjs+recharts. 빌드 그린. 듀얼인증 검증(admin123!→200/오답→401/ENV마스터→200).
- 배포: Vercel https://ss-037-codeatlas-admin.vercel.app, env 6개(op service_role 포함) 주입. 운영 E2E PASS(Playwright 로그인→대시보드 스샷, IP/기기 실데이터).
- 보고서: out/report-codeatlas-admin-20260628.html

### 백엔드 login_event 로깅
- LoginEvent 모델 추가(codeatlas.login_event 매핑, migrate 없음). google/apple/dev/refresh 시 IP(x-forwarded-for)/UA/provider 기록(failsafe). 커밋 652d68c, VPS 배포, dev-login 호출로 IP 캡처 검증(1.219.121.184).

### 구글 로그인 DEVELOPER_ERROR 수정
- 원인: GCP에 Android OAuth 클라이언트 미등록(Web/iOS만 있었음). status 10.
- 수정: GCP codeatlas-500015에 Android 클라이언트 추가(pkg kr.codeatlas.worktimer + debug SHA 5E:8F:16:06:2E:A3:CD:2C:4A:0D:54:78:76:BA:A6:F3:8C:AB:F6:25). 재빌드 불필요.
- ★ Android 구글로그인은 클라이언트ID를 앱에서 안 씀 — pkg+SHA로 매칭, 앱은 webClientId만 사용.
- 검증: qhv147@gmail.com 구글 가입 성공 → codeatlas.users(provider=google) + login_event 1건 → 어드민 표시. 전 체인 라이브.
- USB 함정: 폰이 macOS USB 레벨에서 안 잡힘(충전전용 케이블 의심) → 폰 직접 로그인 + DB로 검증(Plan B).
